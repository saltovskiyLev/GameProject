Исходные данные:
int gold = 30; // золото
int crystals = 1; // кристаллы
int level = 8; // уровень игрока
bool hasQuest = true; // квест получен

Определить значение логических выражений:
1) level > 10 || !hasQuest || gold > 10 && crystals > 0
2) !(hasQuest || gold > 40 && level < 10) && crystals == 1
3) level > 5 && !(crystals > 0 && !(gold > 25 || hasQuest))