*** 1 Сервис дотсупности ЛогИна
*** 2 Сервис Регистрации
*** 3 Сервис аутентифекации
*** 4 Сервис создания приглашения
*** 5 Сервис использования приглашения

-1) Требуется:

// Информация о пользователях(по запросу из базы данных)
// string request = string.Format("SELECT * FROM users WHERE login = '{0}'", login);

// using для экономии памяти, в котором происхрдит создание обекта SqliteDataReader, который является результатом выполнения запроса(тут не очень уверен, но по коду и по памяти я помню, что-то такое)

// проверка на то, что элементы есть
// reader.HasRows

// если такой есть(ЛогИн), то возвращаем false, что значит "такой ЛогИн занят"
// в else, где ЛогИн свободен, мы проходим проверку на индеведуальность ЛогИна 

-2)

// в начале создаем переменную, типа bool, для обозначения успешности регисрации

// SqliteCommand sqliteCommand1 = PrepareCommand(string.Format("INSERT INTO users (login, password, name)" + " VALUES ('{0}', '{1}', '{2}')", login, password, userName));
// этой строкой сохраняем данные о пользователе(тоже не очень уверен)
// sqliteCommand1.ExecuteNonQuery();
// что это за функция я оканачательно забыл

// все это происходит в блоке try дабы избежать exeption
// затем, в catch


-3)

// 
// Пароль Пользователя(который приходит по запросу из базы данных)

-4) Требуется: